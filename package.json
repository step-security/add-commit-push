{
  "name": "add-commit-push",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "ncc build _main.mjs -o dist && cp main.sh dist/"
  },
  "dependencies": {
    "@actions/core": "^2.0.3",
    "axios": "^1.18.1"
  },
  "devDependencies": {
    "@vercel/ncc": "0.44.1"
  }
}
